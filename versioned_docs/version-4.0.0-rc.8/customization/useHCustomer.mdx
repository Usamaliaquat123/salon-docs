---
title: useHCustomer()
sidebar_position: 2
---


```tsx
import { useHCustomer } from '@umar-epacken/saloon-data/src/hooks/customer/useCustomer';

const {
  customers,
    favouriteBeauticians,
    favouriteSaloons,
    favouriteProducts,
    favouriteServices,
    createCustomerFavouriteAPI,
    checkCustomerFavouriteAPI,
    getCustomersBySaloonAPI,
    getOrdersByCustomerAPI,
    getCustomersAPI,
    getCustomerFavouritesAPI,
    deleteCustomerFavouriteAPI,
    createCustomerReviewAPI,
    getCustomerByIdAPI,
    createCustomerAPI,
    getCustomersByBeauticianAPI,
    updateCustomerAPI,
    sendTipAPI,
    sendFeedByCustomerAPI,
    downloadInvoiceAPI,
    deleteCustomerAPI,
    getCustomerOffersAPI,
} = useHCustomer()

```


## getCustomerOffersAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/

## deleteCustomerAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```

## downloadInvoiceAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/

```


## sendFeedByCustomerAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```
## sendTipAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```
## updateCustomerAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```
## getCustomersByBeauticianAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```
## createCustomerAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```
## getCustomerByIdAPI
loading state of the app
```tsx
const { loading } = useHUser()

console.log(loading) /** Loading state of the app **/
```







