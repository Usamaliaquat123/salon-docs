---
title: useHLogin()
sidebar_position: 2
---

```tsx
import { useHLogin } from "@umar-epacken/saloon-data/src/hooks/auth/useLogin";

const { loginAPI } = useHLogin();

const res: any = await loginAPI({
  biometricData: { signature: "8h38h3!9ahdashashd9d9", data: { userId } },
  userType: "customers" /** customers | saloon | beautician **/,
  securityType: "fingerPrint",
});
```

## loginAPI()

this function trigger the login api to save the user in redux by using `setReduxLoginInfo()`
loginApi included 3 parameters

### biometricData

### biometricData

### securityType
